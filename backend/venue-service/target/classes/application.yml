spring:
  application:
    name: venue-service
  datasource:
    url: jdbc:mysql://localhost:3306/kp3?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  redis:
    host: localhost
    port: 6379
    password: 
    database: 0

server:
  port: 8083

logging:
  level:
    root: info
    '[com.kp3]': debug
